App.js (main - root )
        - we have =
        <header/>
        <outlet/> - here as per click about/contact/resto pg will load

        Body
        -- listofresto - displaying main resto cards
            <RestoCard/>

                     <RestoInfo/> = displays name-cuisine-cost for particular resto

                        <MenuCategory / > = displays each resturants menu category mand total of sub items in it

                            <ItemList/ > = displays all items for particular category

